# Core orchestration
langgraph>=0.2.0
langchain>=0.2.0
langchain-anthropic>=0.1.15
langchain-core>=0.2.0

# LLM providers (configurable per node — OpenAI is the default, Anthropic on request)
openai>=1.40.0
anthropic>=0.30.0

# Database — Neon (PostgreSQL)
psycopg2-binary>=2.9.9

# Config
pyyaml>=6.0.1
python-dotenv>=1.0.0

# HTTP (discovery fetches)
requests>=2.32.0

# Gmail API
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.130.0

# Document generation (CV / cover letter)
python-docx>=1.1.0

# Utilities
python-dateutil>=2.9.0

# Testing
pytest>=8.2.0
pytest-mock>=3.14.0
responses>=0.25.0
